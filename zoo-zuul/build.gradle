jar {
    baseName = 'zoo-zuul'
    version = '0.0.1-SNAPSHOT'
}

dependencies {
    compile "org.springframework.cloud:spring-cloud-starter-zuul:${springCouldVersion}"
    compile "org.springframework.cloud:spring-cloud-starter-eureka:${springCouldVersion}"
}