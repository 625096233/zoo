jar {
    baseName = 'zoo-keeper'
    version = '0.0.1-SNAPSHOT'
}

dependencies {
    compile("org.springframework.cloud:spring-cloud-starter-eureka-server:${springCouldVersion}") {
        exclude(module: 'servlet-api')
    }
}