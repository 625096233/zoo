jar {
    baseName = 'zoo-turbine'
    version = '0.0.1-SNAPSHOT'
}

dependencies {
    compile "org.springframework.cloud:spring-cloud-netflix-turbine:${springCouldVersion}"
    compile "org.springframework.cloud:spring-cloud-starter-turbine-amqp:${springCouldVersion}"
}